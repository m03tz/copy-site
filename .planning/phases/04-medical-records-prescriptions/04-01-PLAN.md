---
phase: 04-medical-records-prescriptions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00002_phase4_updates.sql
  - lib/types/database.ts
  - next.config.ts
  - messages/ar.json
  - messages/en.json
autonomous: true

must_haves:
  truths:
    - "Secretary can INSERT and SELECT medical_records (RLS permits)"
    - "Secretary can DELETE patient_files (RLS permits)"
    - "patient_files table has medical_record_id column for visit-linked files"
    - "Server actions accept files up to 10MB without error"
    - "i18n keys exist for patients, visits, prescriptions, and files namespaces"
  artifacts:
    - path: "supabase/migrations/00002_phase4_updates.sql"
      provides: "RLS policy fixes + schema changes for Phase 4"
      contains: "medical_record_id"
    - path: "lib/types/database.ts"
      provides: "Updated TypeScript types with medical_record_id on patient_files"
      contains: "medical_record_id"
    - path: "next.config.ts"
      provides: "bodySizeLimit config for file uploads"
      contains: "bodySizeLimit"
    - path: "messages/ar.json"
      provides: "Arabic translations for patients, visits, prescriptions, files"
      contains: "patients"
    - path: "messages/en.json"
      provides: "English translations for patients, visits, prescriptions, files"
      contains: "patients"
  key_links:
    - from: "lib/types/database.ts"
      to: "supabase/migrations/00002_phase4_updates.sql"
      via: "TypeScript types must mirror SQL schema changes"
      pattern: "medical_record_id.*string"
---

<objective>
Phase 4 foundation: database migration for RLS policy fixes and schema changes, TypeScript type updates, Next.js config for file uploads, install use-debounce dependency, and i18n translations for all Phase 4 namespaces.

Purpose: Without these foundation changes, server actions will fail (wrong RLS policies, missing columns, body size limit), and UI components will have no translation keys. This must complete before any server actions or UI work.
Output: SQL migration file, updated database types, updated next.config.ts, expanded i18n JSON files, use-debounce installed.
</objective>

<execution_context>
@C:\Users\computer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\computer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-medical-records-prescriptions/04-RESEARCH.md
@lib/types/database.ts
@next.config.ts
@messages/ar.json
@messages/en.json
@supabase/migrations/00001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration and update TypeScript types</name>
  <files>
    supabase/migrations/00002_phase4_updates.sql
    lib/types/database.ts
  </files>
  <action>
    Create `supabase/migrations/00002_phase4_updates.sql` with these changes:

    1. **Add medical_record_id to patient_files** (visit-linked files per user decision):
       - `ALTER TABLE patient_files ADD COLUMN medical_record_id UUID REFERENCES medical_records(id);`
       - Create index: `CREATE INDEX idx_patient_files_medical_record_id ON patient_files(medical_record_id);`

    2. **Fix RLS: Secretary access to medical_records** (user decided "Both doctor and secretary can create visit records" and "Visit records are always editable"):
       - Add SELECT policy for secretary on medical_records
       - Add INSERT policy for secretary on medical_records
       - Add UPDATE policy for secretary on medical_records
       - Use `(SELECT get_user_role()) = 'secretary'` pattern matching existing policies

    3. **Fix RLS: Secretary can delete patient_files** (user decided "Both doctor and secretary can delete uploaded files"):
       - Drop existing "Doctor deletes files" policy on patient_files
       - Create new policy "Doctor and secretary delete files" allowing both roles

    4. **Storage RLS policies** (as SQL comments for reference -- bucket creation is manual):
       - Include commented-out storage.objects policies for INSERT (doctor+secretary), SELECT (doctor+secretary+patient own folder), DELETE (doctor+secretary)
       - These serve as documentation; actual storage policies are applied via Supabase Dashboard

    Then update `lib/types/database.ts`:
    - Add `medical_record_id: string | null` to patient_files Row type
    - Add `medical_record_id?: string | null` to patient_files Insert type
    - Add `medical_record_id?: string | null` to patient_files Update type
    - Keep Relationships: [] on patient_files (Supabase v2.95 requirement)
  </action>
  <verify>
    - File `supabase/migrations/00002_phase4_updates.sql` exists and contains ALTER TABLE, CREATE POLICY statements
    - `lib/types/database.ts` compiles: `pnpm exec tsc --noEmit lib/types/database.ts` passes (or check with full build)
    - Grep confirms `medical_record_id` appears in both SQL and TypeScript files
  </verify>
  <done>
    SQL migration covers: medical_record_id column addition, 3 secretary RLS policies on medical_records, updated DELETE policy on patient_files. TypeScript types mirror the schema change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Next.js config, install use-debounce, and add i18n translations</name>
  <files>
    next.config.ts
    messages/ar.json
    messages/en.json
  </files>
  <action>
    1. **Update next.config.ts** to increase server action body size limit for medical file uploads:
       ```typescript
       const nextConfig: NextConfig = {
         experimental: {
           serverActions: {
             bodySizeLimit: '10mb',
           },
         },
       };
       ```
       Keep the existing withNextIntl wrapper intact.

    2. **Install use-debounce**: Run `pnpm add use-debounce`

    3. **Add i18n translations** to both ar.json and en.json. Add these new namespace sections (preserve all existing keys):

       **patients namespace** (patient list, search, profile):
       - title, search (placeholder, noResults), card labels (phone, dateOfBirth, bloodType, lastVisit), profile tabs (info, visits, prescriptions, files), info form labels (fullNameAr, fullNameEn, phone, email, dateOfBirth, bloodType, nationalId, emergencyContactName, emergencyContactPhone, notes), actions (save, edit, cancel), pagination (previous, next, page, of), empty states, sort label

       **visits namespace** (visit records):
       - title, create, edit, form labels (visitDate, appointment, notes, selectAppointment, noAppointment, walkIn), card labels (visitDate, doctor, notes), actions (save, cancel, delete), empty state, confirmDelete

       **prescriptions namespace**:
       - title, create, form labels (medicationName, dosage, duration, instructions, addMedication, removeMedication), card labels (medications, createdAt), print (title, printButton, clinicName, doctorName, specialty, phone, address, patient, date, medication, dosage, duration, instructions, signature), empty state, actions

       **files namespace** (file management):
       - title, upload (title, selectFile, uploading, allowedTypes, maxSize), list labels (fileName, uploadDate, fileType, uploadedBy), types (image, pdf), actions (download, delete), confirmDelete, empty state

       All Arabic translations should be proper Arabic medical/clinical terminology. Use the same translation key style as existing namespaces (camelCase keys).
  </action>
  <verify>
    - `pnpm build` completes without errors (validates next.config.ts and JSON files)
    - `pnpm list use-debounce` shows the package is installed
    - ar.json and en.json both contain "patients", "visits", "prescriptions", "files" top-level keys
  </verify>
  <done>
    next.config.ts has bodySizeLimit: '10mb'. use-debounce is installed. Both ar.json and en.json have complete translation keys for patients, visits, prescriptions, and files namespaces covering all UI labels, form fields, actions, and empty states.
  </done>
</task>

</tasks>

<verification>
- SQL migration file exists with correct ALTER TABLE and CREATE POLICY statements
- TypeScript types compile successfully
- next.config.ts has serverActions.bodySizeLimit: '10mb'
- use-debounce package installed
- Both translation files have 4 new namespaces (patients, visits, prescriptions, files)
- `pnpm build` passes
</verification>

<success_criteria>
- Database migration ready to apply (not applied yet -- just the file)
- TypeScript types updated to include medical_record_id on patient_files
- File upload limit configured to 10MB
- All i18n keys exist for Phase 4 UI work
- Project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-medical-records-prescriptions/04-01-SUMMARY.md`
</output>
