---
phase: 05-pregnancy-tracking
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - components/pregnancy/pregnancy-list.tsx
  - components/pregnancy/pregnancy-card.tsx
  - components/pregnancy/pregnancy-form.tsx
  - components/pregnancy/measurement-form.tsx
  - components/pregnancy/measurement-list.tsx
  - app/[locale]/doctor/patients/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Doctor can create a pregnancy record with LMP date from the patient profile page"
    - "Doctor can update pregnancy status (active, delivered, miscarriage, ectopic)"
    - "Doctor can add per-visit measurements (weight, blood pressure, fetal heartbeat, gestational week)"
    - "Doctor sees current gestational week auto-calculated and displayed on active pregnancies"
    - "Doctor sees all pregnancies for a patient (multiple allowed, most recent first)"
    - "Pregnancy tab only appears on doctor patient profile (NOT secretary)"
  artifacts:
    - path: "components/pregnancy/pregnancy-list.tsx"
      provides: "Container listing all pregnancies with add button"
    - path: "components/pregnancy/pregnancy-card.tsx"
      provides: "Single pregnancy display with status, gestational week, measurements"
    - path: "components/pregnancy/pregnancy-form.tsx"
      provides: "Create pregnancy form with LMP date input"
    - path: "components/pregnancy/measurement-form.tsx"
      provides: "Add measurement form with weight, BP, heartbeat, week"
    - path: "components/pregnancy/measurement-list.tsx"
      provides: "Read-only table of measurements per pregnancy"
    - path: "app/[locale]/doctor/patients/[id]/page.tsx"
      provides: "Modified page with 5th pregnancy tab"
      contains: "TabsTrigger.*pregnancy"
  key_links:
    - from: "components/pregnancy/pregnancy-form.tsx"
      to: "lib/actions/pregnancies.ts"
      via: "createPregnancy server action"
      pattern: "createPregnancy"
    - from: "components/pregnancy/measurement-form.tsx"
      to: "lib/actions/pregnancies.ts"
      via: "addMeasurement server action"
      pattern: "addMeasurement"
    - from: "components/pregnancy/pregnancy-card.tsx"
      to: "lib/utils/pregnancy.ts"
      via: "getGestationalWeek for display"
      pattern: "getGestationalWeek"
    - from: "app/[locale]/doctor/patients/[id]/page.tsx"
      to: "components/pregnancy/pregnancy-list.tsx"
      via: "PregnancyList component in tab"
      pattern: "PregnancyList"
---

<objective>
Build the doctor-facing pregnancy management UI: pregnancy components (list, card, form, measurement form, measurement list) and integrate as 5th tab on the doctor's patient profile page.

Purpose: Satisfies PREG-01 (doctor records pregnancy with LMP) and PREG-02 (doctor records per-visit measurements). This is the core clinical workflow.
Output: 5 components in `components/pregnancy/`, modified `app/[locale]/doctor/patients/[id]/page.tsx`
</objective>

<execution_context>
@C:\Users\computer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\computer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pregnancy-tracking/05-RESEARCH.md
@.planning/phases/05-pregnancy-tracking/05-01-SUMMARY.md
@lib/actions/pregnancies.ts
@lib/utils/pregnancy.ts
@lib/types/database.ts
@app/[locale]/doctor/patients/[id]/page.tsx
@components/prescriptions/prescription-form.tsx
@components/medical-records/visit-form.tsx
@messages/ar.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pregnancy components (list, card, forms, measurement list)</name>
  <files>
    components/pregnancy/pregnancy-list.tsx,
    components/pregnancy/pregnancy-card.tsx,
    components/pregnancy/pregnancy-form.tsx,
    components/pregnancy/measurement-form.tsx,
    components/pregnancy/measurement-list.tsx
  </files>
  <action>
Create 5 components in `components/pregnancy/`. All are Client Components ('use client') since they involve interactive forms and state.

**1. `pregnancy-form.tsx` — Create pregnancy dialog form**

- Uses react-hook-form + zodResolver (same pattern as PrescriptionForm in `components/prescriptions/prescription-form.tsx`)
- Zod schema: `{ lmp_date: z.string().min(1), notes: z.string().optional() }`
- Props: `{ patientId: string }`
- Renders inside a shadcn Dialog (trigger button "Add Pregnancy" passed from parent)
- Fields: LMP date (Input type="date"), Notes (Textarea, optional)
- On submit: construct FormData with patient_id (from props) + lmp_date + notes, call `createPregnancy` server action
- Show loading state on submit button, close dialog on success, show error via inline message
- Uses `useTranslations('pregnancy')` for labels
- Do NOT include expected_due_date field — it is GENERATED ALWAYS in DB

**2. `measurement-form.tsx` — Add measurement dialog form**

- Uses react-hook-form + zodResolver
- Zod schema matches the server action schema (all optional except measured_at):
  ```
  {
    measured_at: z.string().min(1),
    gestational_week: z.coerce.number().int().min(1).max(42).optional().or(z.literal('')),
    weight_kg: z.coerce.number().positive().optional().or(z.literal('')),
    blood_pressure: z.string().optional(),
    fetal_heartbeat: z.coerce.number().int().min(60).max(200).optional().or(z.literal('')),
    notes: z.string().optional()
  }
  ```
- Props: `{ pregnancyId: string }`
- Fields: measured_at (Input type="date", default today), gestational_week (Input type="number"), weight_kg (Input type="number" step="0.01"), blood_pressure (Input type="text" placeholder="120/80"), fetal_heartbeat (Input type="number"), notes (Textarea)
- On submit: construct FormData — for optional numeric fields, only include if value is truthy/non-empty. Call `addMeasurement` server action.
- gestational_week is DOCTOR-ENTERED, not auto-calculated. Label should make this clear.
- Uses `useTranslations('pregnancy')` for labels from `measurements.*` keys

**3. `measurement-list.tsx` — Read-only measurement table**

- Props: `{ measurements: PregnancyMeasurement[] }` (import type from `lib/types/database.ts`)
- Renders a table with columns: Date, Week, Weight (kg), Blood Pressure, Fetal Heartbeat (bpm), Notes
- Sorted by measured_at descending (most recent first) — sort in component
- Use date-fns `format` for date display
- Empty state: show measurements.empty translation
- Uses `useTranslations('pregnancy')` for column headers from `measurements.*` keys
- Client Component ('use client') for translations

**4. `pregnancy-card.tsx` — Single pregnancy expandable card**

- Props: `{ pregnancy: Pregnancy & { pregnancy_measurements: PregnancyMeasurement[] } }`
- Displays:
  - Status badge (colored: active=green, delivered=blue, miscarriage/ectopic=red) using `pregnancy.status` translations
  - LMP date (formatted with date-fns)
  - Expected due date (from pregnancy.expected_due_date, formatted)
  - Current gestational week: call `getGestationalWeek(pregnancy.lmp_date)` — only show for active pregnancies, display as "Week X" with translation
  - Days until due: call `getDaysUntilDue(pregnancy.expected_due_date)` — only show for active pregnancies
  - Notes (if any)
- Status update: A Select dropdown with the 4 status values. On change, call `updatePregnancyStatus` with pregnancy_id + new status via FormData. Only show if current status is 'active'.
- "Add Measurement" button opens MeasurementForm dialog (pass pregnancyId)
- Below the card header, render MeasurementList with pregnancy.pregnancy_measurements
- Use shadcn Card, Badge, Select, Button, Separator
- Client Component ('use client')

**5. `pregnancy-list.tsx` — Container for all pregnancies**

- Props: `{ pregnancies: (Pregnancy & { pregnancy_measurements: PregnancyMeasurement[] })[], patientId: string }`
- Renders "Add Pregnancy" button at top which opens PregnancyForm dialog
- Maps over pregnancies (already sorted by created_at desc from server) and renders PregnancyCard for each
- Empty state: show noPregnancies translation with an icon
- Client Component ('use client')

**Styling notes:**
- Use Tailwind classes consistent with existing components (space-y-4, text-sm, etc.)
- Use logical CSS properties (ps/pe/ms/me, text-start) for RTL compatibility
- Badge variants: use "default" for active (green via className), "secondary" for delivered, "destructive" for miscarriage/ectopic
  </action>
  <verify>
- `npx tsc --noEmit` passes (all components type-check)
- All 5 files exist in components/pregnancy/
- pregnancy-form.tsx imports and calls createPregnancy from lib/actions/pregnancies.ts
- measurement-form.tsx imports and calls addMeasurement from lib/actions/pregnancies.ts
- pregnancy-card.tsx imports getGestationalWeek from lib/utils/pregnancy.ts
- No reference to expected_due_date in any form input/submission
- All components use useTranslations('pregnancy')
  </verify>
  <done>5 pregnancy components created: pregnancy-list, pregnancy-card, pregnancy-form, measurement-form, measurement-list — all type-check and use established patterns (react-hook-form, shadcn/ui, i18n)</done>
</task>

<task type="auto">
  <name>Task 2: Integrate pregnancy tab into doctor patient profile page</name>
  <files>app/[locale]/doctor/patients/[id]/page.tsx</files>
  <action>
Modify `app/[locale]/doctor/patients/[id]/page.tsx` to add pregnancy as a 5th tab.

**Changes needed:**

1. **Add import** for PregnancyList:
   ```typescript
   import { PregnancyList } from '@/components/pregnancy/pregnancy-list'
   ```

2. **Add pregnancy data fetch** alongside existing queries (after the patientFiles query):
   ```typescript
   const { data: pregnancyData } = await supabase
     .from('pregnancies')
     .select('*, pregnancy_measurements(*)')
     .eq('patient_id', id)
     .order('created_at', { ascending: false })
   ```
   Type the result appropriately using the Pregnancy and PregnancyMeasurement types from database.ts. For each pregnancy, order pregnancy_measurements by measured_at descending.

   NOTE: Supabase nested ordering on joined tables is limited. After fetching, sort measurements in JS:
   ```typescript
   const pregnancies = (pregnancyData ?? []).map(p => ({
     ...p,
     pregnancy_measurements: [...(p.pregnancy_measurements ?? [])].sort(
       (a, b) => new Date(b.measured_at).getTime() - new Date(a.measured_at).getTime()
     )
   }))
   ```

3. **Change TabsList grid** from `grid-cols-4` to `grid-cols-5`

4. **Add TabsTrigger** after the files trigger:
   ```typescript
   <TabsTrigger value="pregnancy">{t('profile.tabs.pregnancy')}</TabsTrigger>
   ```

5. **Add TabsContent** after the files content:
   ```typescript
   <TabsContent value="pregnancy" className="mt-6">
     <PregnancyList
       pregnancies={pregnancies}
       patientId={id}
     />
   </TabsContent>
   ```

6. **Also add translations import** — the page already uses `getTranslations('patients')` which covers `patients.profile.tabs.pregnancy` added in Plan 01.

This change is ONLY for the doctor's patient profile page. Do NOT modify any secretary page. The secretary has no access to pregnancy data (RLS blocks it, and we simply don't add the tab).
  </action>
  <verify>
- `npx tsc --noEmit` passes
- `app/[locale]/doctor/patients/[id]/page.tsx` contains `grid-cols-5`
- `app/[locale]/doctor/patients/[id]/page.tsx` contains TabsTrigger with value="pregnancy"
- `app/[locale]/doctor/patients/[id]/page.tsx` imports PregnancyList
- `app/[locale]/doctor/patients/[id]/page.tsx` queries `pregnancies` table with `pregnancy_measurements(*)`
- No changes to any secretary page
  </verify>
  <done>Doctor patient profile page has 5 tabs (info, visits, prescriptions, files, pregnancy). Pregnancy tab shows PregnancyList with full CRUD: create pregnancy, update status, add measurements, view measurement history.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Doctor patient profile has 5 tabs with pregnancy as 5th tab
- All pregnancy components follow established patterns (react-hook-form, shadcn/ui, i18n)
- No pregnancy tab on secretary patient profile page
- PREG-01 satisfied: doctor can create pregnancy with LMP, sees auto-calculated gestational weeks and expected due date
- PREG-02 satisfied: doctor can add per-visit measurements (weight, BP, heartbeat)
</verification>

<success_criteria>
- Doctor can create a pregnancy record from the Pregnancy tab on a patient profile
- Pregnancy card shows LMP, expected due date (from DB), current gestational week (calculated), status
- Doctor can update pregnancy status via dropdown
- Doctor can add measurements and see them listed in the card
- All text displays in Arabic and English correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-pregnancy-tracking/05-02-SUMMARY.md`
</output>
