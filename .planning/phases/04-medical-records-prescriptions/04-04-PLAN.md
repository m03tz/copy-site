---
phase: 04-medical-records-prescriptions
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - components/medical-records/visit-form.tsx
  - components/medical-records/visit-card.tsx
  - components/medical-records/visit-list.tsx
  - components/prescriptions/prescription-form.tsx
  - components/prescriptions/prescription-card.tsx
  - components/prescriptions/prescription-list.tsx
  - components/prescriptions/prescription-print.tsx
  - app/globals.css
autonomous: true

must_haves:
  truths:
    - "Doctor or secretary can create a visit record with date and free-text notes"
    - "Doctor or secretary can edit an existing visit record"
    - "Doctor can create a prescription with multiple medications for a visit"
    - "Prescription prints on A4 with clinic header, patient name, date, and medication table in Arabic RTL"
    - "Visit records display in chronological order on the visits tab"
    - "Prescriptions display grouped by visit on the prescriptions tab"
  artifacts:
    - path: "components/medical-records/visit-form.tsx"
      provides: "Create/edit visit record form with notes-only textarea"
      contains: "createVisitRecord"
    - path: "components/medical-records/visit-card.tsx"
      provides: "Visit record display card"
      contains: "Card"
    - path: "components/medical-records/visit-list.tsx"
      provides: "Chronological list of visit records"
      contains: "VisitCard"
    - path: "components/prescriptions/prescription-form.tsx"
      provides: "Multi-medication prescription form using useFieldArray"
      contains: "useFieldArray"
    - path: "components/prescriptions/prescription-card.tsx"
      provides: "Prescription display card with print button"
      contains: "Card"
    - path: "components/prescriptions/prescription-print.tsx"
      provides: "Printable A4 RTL prescription layout"
      contains: "prescription-print"
    - path: "app/globals.css"
      provides: "Print media queries for prescription"
      contains: "@media print"
  key_links:
    - from: "components/medical-records/visit-form.tsx"
      to: "lib/actions/medical-records.ts"
      via: "createVisitRecord / updateVisitRecord server action"
      pattern: "createVisitRecord|updateVisitRecord"
    - from: "components/prescriptions/prescription-form.tsx"
      to: "lib/actions/prescriptions.ts"
      via: "createPrescription server action"
      pattern: "createPrescription"
    - from: "components/prescriptions/prescription-print.tsx"
      to: "window.print()"
      via: "CSS @media print hides everything except .prescription-print"
      pattern: "window\\.print"
---

<objective>
Build the visit records UI (create/edit/view) and prescriptions UI (create with multi-medication form, view, printable A4 RTL layout) and wire them into the patient profile tabs.

Purpose: This is the core clinical workflow -- the doctor creates visit records with notes and prescriptions for each patient visit. The printable prescription is a key deliverable (clinic branding, A4, Arabic RTL).
Output: Visit form/card/list components, prescription form/card/list/print components, print CSS, wired into patient profile visits and prescriptions tabs.
</objective>

<execution_context>
@C:\Users\computer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\computer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-medical-records-prescriptions/04-RESEARCH.md
@.planning/phases/04-medical-records-prescriptions/04-03-SUMMARY.md
@lib/actions/medical-records.ts
@lib/actions/prescriptions.ts
@lib/types/database.ts
@app/[locale]/doctor/patients/[id]/page.tsx
@app/[locale]/secretary/patients/[id]/page.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create visit record components and wire into patient profile</name>
  <files>
    components/medical-records/visit-form.tsx
    components/medical-records/visit-card.tsx
    components/medical-records/visit-list.tsx
    app/[locale]/doctor/patients/[id]/page.tsx
    app/[locale]/secretary/patients/[id]/page.tsx
  </files>
  <action>
    **components/medical-records/visit-form.tsx** (Client Component):
    - 'use client' directive
    - Form with: visit_date (date input, defaults to today), notes (Textarea -- single free-text area per user decision "Notes only"), optional appointment link (Select dropdown of patient's unlinked appointments, or "Walk-in / No appointment" option per user decision "supports walk-ins")
    - Hidden input for patient_id
    - On submit: call createVisitRecord or updateVisitRecord server action via FormData
    - Edit mode: pre-fill date and notes from existing record
    - Show loading state during submission, success/error feedback
    - Use translations from 'visits' namespace
    - DO NOT add fields for chief_complaint, diagnosis, treatment_plan, vital_signs (user decided "notes only")

    **components/medical-records/visit-card.tsx**:
    - Display a visit record: visit_date (formatted with date-fns), notes (truncated preview or full), optional linked appointment info
    - Edit button (opens VisitForm in edit mode via Dialog or inline toggle)
    - Show prescription count if prescriptions exist for this visit
    - "Add Prescription" button (opens PrescriptionForm -- can be a Dialog triggered from here)

    **components/medical-records/visit-list.tsx**:
    - Renders a list of VisitCard components in reverse chronological order
    - "New Visit Record" button at top that opens VisitForm (Dialog or inline)
    - Empty state: message indicating no visit records yet
    - Accept visits array (with embedded prescriptions) and patientId as props

    **Update patient profile pages** (both doctor and secretary):
    - Replace visits tab placeholder with VisitList component, passing the already-fetched visits data
    - Replace prescriptions tab placeholder -- prescriptions will be handled in the next task (prescription-list) but wire the data through now
  </action>
  <verify>
    - All three visit components exist under components/medical-records/
    - visit-form.tsx does NOT contain chief_complaint, diagnosis, treatment_plan, or vital_signs
    - Patient profile page visits tab renders VisitList
    - `pnpm build` passes
  </verify>
  <done>
    Visit form creates records with date + notes only. Visit cards display records with edit and prescription links. Visit list shows chronological records with "New Visit Record" action. Both doctor and secretary profile pages show visits in the visits tab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create prescription components with printable layout and print CSS</name>
  <files>
    components/prescriptions/prescription-form.tsx
    components/prescriptions/prescription-card.tsx
    components/prescriptions/prescription-list.tsx
    components/prescriptions/prescription-print.tsx
    app/globals.css
  </files>
  <action>
    **components/prescriptions/prescription-form.tsx** (Client Component):
    - 'use client' directive
    - Uses react-hook-form with zodResolver for form validation
    - Uses useFieldArray for dynamic medication rows (per research: "react-hook-form useFieldArray for dynamic medication inputs")
    - Each medication row: medication_name (Input), dosage (Input), duration (Input), instructions (Input, optional)
    - "Add Medication" button (Plus icon) appends a new row
    - Remove button (Trash2 icon) on each row (minimum 1 medication, hide remove on last)
    - On submit: call createPrescription server action with { medical_record_id, patient_id, medications[] }
    - Start with one empty medication row by default
    - Success/error feedback, loading state
    - Use translations from 'prescriptions' namespace

    **components/prescriptions/prescription-card.tsx**:
    - Displays a group of prescription medications for one visit
    - Shows: visit date (from parent medical record), list of medications with dosage/duration/instructions
    - "Print" button that opens prescription print view
    - Use a mini table or structured layout for medication display

    **components/prescriptions/prescription-list.tsx**:
    - Groups prescription rows by medical_record_id (since each medication is a separate DB row)
    - Renders PrescriptionCard for each group
    - Accept prescriptions data (flattened from all visits) and patient info as props
    - Empty state message

    **components/prescriptions/prescription-print.tsx** (Client Component):
    - 'use client' directive
    - Full A4 printable prescription layout (per user decisions):
      - Header: clinic logo (from public/clinic-logo.png if exists, graceful fallback), clinic name, doctor name, specialty, phone (dir="ltr"), address
      - Patient info: patient name + prescription date only (per user decision "no age")
      - Medications table: medication name | dosage | duration | instructions (structured table per user decision)
      - Doctor signature area at bottom
    - Entire layout wrapped in div with className="prescription-print" dir="rtl" lang="ar"
    - Print button calls window.print()
    - Clinic info can be hardcoded for v1 (Dr. Fadi's clinic details from PROJECT.md) or passed as props

    **app/globals.css** -- Add print media queries (append to end of existing file):
    ```css
    @media print {
      body * {
        visibility: hidden;
      }
      .prescription-print,
      .prescription-print * {
        visibility: visible;
      }
      .prescription-print {
        position: absolute;
        inset-inline-start: 0;
        top: 0;
        width: 100%;
      }
      .no-print {
        display: none !important;
      }
      @page {
        size: A4 portrait;
        margin: 15mm 20mm;
      }
    }
    ```

    **Wire prescriptions tab** in patient profile pages:
    - Update the prescriptions tab in doctor and secretary profile pages to render PrescriptionList with the fetched prescription data grouped from visits
  </action>
  <verify>
    - All four prescription components exist under components/prescriptions/
    - prescription-form.tsx imports useFieldArray from react-hook-form
    - prescription-print.tsx contains "prescription-print" className and window.print()
    - globals.css contains @media print rules
    - `pnpm build` passes
  </verify>
  <done>
    Prescription form allows adding multiple medications with dynamic rows. Prescription cards display medication groups with print button. Printable prescription renders A4 RTL layout with clinic header, patient name, date, medication table, and signature area. Print CSS hides everything except the prescription. Prescriptions tab in patient profile shows grouped prescriptions.
  </done>
</task>

</tasks>

<verification>
- Visit form creates a record with date + notes only (no structured fields)
- Edit button on visit card allows updating notes
- Prescription form has dynamic medication rows (add/remove)
- Print button opens browser print dialog showing A4 prescription
- Print layout shows clinic header, patient name + date, medication table in RTL
- Both tabs (visits, prescriptions) populated in patient profile for doctor and secretary
- `pnpm build` passes
</verification>

<success_criteria>
- Visit records: create with notes-only textarea, edit existing, chronological list
- Prescriptions: multi-medication form with useFieldArray, grouped display by visit
- Print: A4 RTL prescription with clinic branding (logo, name, doctor, specialty, phone, address), patient name + date, medication table, signature area
- All wired into patient profile tabs
</success_criteria>

<output>
After completion, create `.planning/phases/04-medical-records-prescriptions/04-04-SUMMARY.md`
</output>
