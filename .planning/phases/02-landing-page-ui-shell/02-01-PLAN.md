---
phase: 02-landing-page-ui-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - messages/ar.json
  - messages/en.json
  - app/[locale]/(public)/layout.tsx
  - app/[locale]/(public)/(auth)/login/page.tsx
  - components/landing/navigation.tsx
  - components/landing/footer.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page has calm medical aesthetic with blue/green tones"
    - "Public pages share a navigation bar with clinic name, section links, login button, and language toggle"
    - "Public pages share a footer with clinic name and copyright"
    - "Navigation is responsive with hamburger menu on mobile"
    - "All landing page text is available in both Arabic and English"
  artifacts:
    - path: "app/globals.css"
      provides: "Medical color palette CSS variables"
      contains: "--medical-blue"
    - path: "messages/ar.json"
      provides: "Arabic landing page translations"
      contains: "landing"
    - path: "messages/en.json"
      provides: "English landing page translations"
      contains: "landing"
    - path: "app/[locale]/(public)/layout.tsx"
      provides: "Public route group layout with navigation and footer"
      min_lines: 15
    - path: "components/landing/navigation.tsx"
      provides: "Responsive public navigation bar"
      min_lines: 40
    - path: "components/landing/footer.tsx"
      provides: "Public footer with clinic info"
      min_lines: 15
  key_links:
    - from: "app/[locale]/(public)/layout.tsx"
      to: "components/landing/navigation.tsx"
      via: "import PublicNavigation"
      pattern: "import.*PublicNavigation.*navigation"
    - from: "app/[locale]/(public)/layout.tsx"
      to: "components/landing/footer.tsx"
      via: "import Footer"
      pattern: "import.*Footer.*footer"
    - from: "components/landing/navigation.tsx"
      to: "/login"
      via: "Link href to login"
      pattern: "href.*login"
---

<objective>
Set up the medical theme, landing page translations, and public route group layout with responsive navigation and footer.

Purpose: Establishes the visual foundation (medical color palette), content foundation (i18n translations), and structural foundation (public layout with shared nav/footer) needed by the landing page components in Plan 02.

Output: Medical-themed CSS variables, complete Arabic+English landing page translations, a (public) route group layout wrapping navigation and footer around public content, and the existing (auth)/login page relocated under (public).
</objective>

<execution_context>
@C:\Users\computer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\computer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page-ui-shell/02-RESEARCH.md

# Existing files to understand current structure
@app/globals.css
@app/[locale]/layout.tsx
@app/[locale]/page.tsx
@messages/ar.json
@messages/en.json
@components/language-toggle.tsx
@middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add medical color palette and landing page translations</name>
  <files>
    app/globals.css
    messages/ar.json
    messages/en.json
  </files>
  <action>
**globals.css -- Add medical color CSS custom properties:**

In the `:root` block of `app/globals.css`, add medical color variables using oklch values that correspond to a calm medical palette (light blues and greens). Add these AFTER the existing shadcn/ui variables:

```
--medical-blue-50: oklch(0.97 0.014 254);
--medical-blue-100: oklch(0.932 0.032 255);
--medical-blue-500: oklch(0.623 0.214 259);
--medical-blue-600: oklch(0.546 0.245 262);
--medical-blue-700: oklch(0.488 0.243 264);
--medical-green-50: oklch(0.982 0.018 155);
--medical-green-100: oklch(0.962 0.044 156);
--medical-green-500: oklch(0.723 0.219 149);
--medical-teal-50: oklch(0.984 0.014 180);
--medical-teal-500: oklch(0.704 0.14 182);
```

Also add corresponding Tailwind theme mappings in the `@theme inline` block:
```
--color-medical-blue-50: var(--medical-blue-50);
--color-medical-blue-100: var(--medical-blue-100);
--color-medical-blue-500: var(--medical-blue-500);
--color-medical-blue-600: var(--medical-blue-600);
--color-medical-blue-700: var(--medical-blue-700);
--color-medical-green-50: var(--medical-green-50);
--color-medical-green-100: var(--medical-green-100);
--color-medical-green-500: var(--medical-green-500);
--color-medical-teal-50: var(--medical-teal-50);
--color-medical-teal-500: var(--medical-teal-500);
```

This allows using classes like `bg-medical-blue-600`, `text-medical-green-500`, etc. in Tailwind.

**messages/ar.json -- Add landing page translation keys:**

Add a new top-level `"landing"` section with these keys:
```json
"landing": {
  "clinicName": "عيادة د. فادي نادي السحلة",
  "heroTitle": "أخصائي نساء وتوليد وعقم وجراحة بالمنظار",
  "heroSubtitle": "نقدم رعاية صحية متكاملة للمرأة في بيئة مريحة وآمنة",
  "bookAppointment": "احجزي موعدك",
  "aboutDoctor": "عن الطبيب",
  "aboutDoctorText": "د. فادي نادي السحلة , حاصل على بكالوريوس الطب والجراحة من جامعة العلوم والتكنولوجيا الأردنية، وحاصل على البورد الأردني في أمراض النساء والتوليد والجزء الأول من البورد البريطاني. عمل سابقاً في مستشفى الملك عبدالله الجامعي.",
  "servicesTitle": "خدماتنا",
  "serviceObgyn": "استشارات نسائية وتوليد",
  "serviceObgynDesc": "فحوصات شاملة للنساء والمتابعة الطبية المتخصصة",
  "servicePregnancy": "متابعة الحمل",
  "servicePregnancyDesc": "متابعة دقيقة للحمل والجنين مع أحدث الأجهزة",
  "serviceInfertility": "علاج العقم",
  "serviceInfertilityDesc": "تشخيص وعلاج مشاكل الإنجاب بأحدث الطرق",
  "serviceSurgery": "جراحة بالمنظار",
  "serviceSurgeryDesc": "عمليات جراحية بتقنية المنظار بأقل تدخل جراحي",
  "contactTitle": "تواصلي معنا",
  "address": "العنوان",
  "clinicAddress": "جرش، وسط البلد، جرش _شارع الشعب (الذهب ) مقابل غرفة تجارة جرش ",
  "phone": "الهاتف",
  "clinicPhone": "+962 7 8663 7847",
  "workingHours": "ساعات العمل",
  "weekdayHours": "الاثنين , الاربعاء , السبت : 10 صباح - 2 مساء",
  "bookingCTA": "هل تريدين حجز موعد؟",
  "bookingDescription": "سجلي الدخول لحجز موعدك مع الطبيب",
  "bookNow": "سجلي الدخول",
  "footerRights": "جميع الحقوق محفوظة",
  "navAbout": "عن الطبيب",
  "navServices": "الخدمات",
  "navContact": "تواصلي معنا",
  "navLogin": "تسجيل الدخول"
}
```

**messages/en.json -- Add matching English translations:**

Add a matching `"landing"` section:
```json
"landing": {
  "clinicName": "Dr. Fadi Nadi Al-Sahleh Clinic",
  "heroTitle": "Specialist in obstetrics and gynecology, Infertility & Laparoscopic Surgery",
  "heroSubtitle": "Providing comprehensive women's healthcare in a comfortable and safe environment",
  "bookAppointment": "Book Appointment",
  "aboutDoctor": "About the Doctor",
  "aboutDoctorText": "Dr. Fadi Nadi Al-Sahleh holds a Bachelor of Medicine and Surgery from Jordan University of Science and Technology (JUST), the Jordanian Board in obstetrics and gynecology, and Part 1 of the British Board. He previously worked at King Abdullah University Hospital.",
  "servicesTitle": "Our Services",
  "serviceObgyn": "obstetrics and gynecology Consultations",
  "serviceObgynDesc": "Comprehensive women's health exams and specialized medical follow-up",
  "servicePregnancy": "Pregnancy Care",
  "servicePregnancyDesc": "Careful pregnancy and fetal monitoring with the latest equipment",
  "serviceInfertility": "Infertility Treatment",
  "serviceInfertilityDesc": "Diagnosis and treatment of reproductive issues using modern methods",
  "serviceSurgery": "Laparoscopic Surgery",
  "serviceSurgeryDesc": "Minimally invasive surgical procedures using laparoscopic techniques",
  "contactTitle": "Contact Us",
  "address": "Address",
  "clinicAddress": "Jarash، Downtown، AlSha'ab Street _ opposite of Jersh Chamber of Commerce",
  "phone": "Phone",
  "clinicPhone": "+962 7 8663 7847",
  "workingHours": "Working Hours",
  "weekdayHours": "Tuesday, Thursday, and Saturday: 10 AM - 2 PM",
  "bookingCTA": "Want to book an appointment?",
  "bookingDescription": "Sign in to book your appointment with the doctor",
  "bookNow": "Sign In",
  "footerRights": "All rights reserved",
  "navAbout": "About",
  "navServices": "Services",
  "navContact": "Contact",
  "navLogin": "Sign In"
}
```

Keep ALL existing keys in both files. Only ADD the new "landing" section.
  </action>
  <verify>
Run `pnpm build` to confirm no JSON syntax errors in translation files and CSS parses correctly. Verify both messages files contain the "landing" key by checking their content.
  </verify>
  <done>
globals.css has medical color CSS variables. Both ar.json and en.json have complete "landing" sections with matching keys for clinic name, hero, services (4), contact info, footer, and navigation labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create (public) route group layout with responsive navigation and footer</name>
  <files>
    app/[locale]/(public)/layout.tsx
    components/landing/navigation.tsx
    components/landing/footer.tsx
  </files>
  <action>
**IMPORTANT structural change:** The current `app/[locale]/page.tsx` (placeholder home page) sits at the root of `[locale]`. The `(auth)/login/page.tsx` already exists. We need to:

1. Create `app/[locale]/(public)/layout.tsx` -- the public layout wrapping navigation + footer
2. Move `app/[locale]/(auth)/` to `app/[locale]/(public)/(auth)/` so login lives under the public layout
3. The existing `app/[locale]/page.tsx` will be replaced by `app/[locale]/(public)/page.tsx` in Plan 02 (delete the old one, create the new one there)

**components/landing/navigation.tsx:**

Create a `'use client'` component `PublicNavigation` that:
- Uses `useTranslations('landing')` for navigation labels
- Has sticky top positioning: `sticky top-0 z-50 border-b bg-white/95 backdrop-blur-sm shadow-sm`
- Contains a max-w-7xl centered container with px-4 padding
- Desktop nav (hidden below md): flex row with clinic name (Link to "/"), section anchor links (#about, #services, #contact) using `t('navAbout')`, `t('navServices')`, `t('navContact')`, a login Link (href="/login") styled as medical-blue-600 button, and `<LanguageToggle />`
- Mobile nav (visible below md): hamburger button using Menu/X icons from lucide-react, useState toggle
- Mobile menu (conditional render when open): vertical stack of same links, each calls setIsOpen(false) on click
- Use LOGICAL properties everywhere: `ms-4` not `ml-4`, `me-4` not `mr-4`, `text-start` not `text-left`, `pe-4` not `pr-4`
- Anchor links should use `<a href="#about">` (not Link), since they're same-page anchors
- Login link uses `<Link href="/login">` from `@/i18n/routing`
- Import LanguageToggle from `@/components/language-toggle`
- Color the nav items with `text-gray-700 hover:text-medical-blue-600 transition-colors`
- Login button: `px-4 py-2 bg-medical-blue-600 text-white rounded-lg hover:bg-medical-blue-700 transition-colors`
- Clinic name should use `text-medical-blue-700 font-bold text-lg`

**components/landing/footer.tsx:**

Create a Server Component (NO 'use client') `Footer` that:
- Uses `getTranslations('landing')` from 'next-intl/server'
- Has a simple footer with `bg-gray-900 text-white py-8`
- Contains clinic name and copyright: `{t('clinicName')} - {new Date().getFullYear()} {t('footerRights')}`
- Centered text in a max-w-7xl container

**app/[locale]/(public)/layout.tsx:**

Create a Server Component layout that:
- Imports PublicNavigation and Footer
- Renders: `<PublicNavigation />` then `<main className="min-h-screen">{children}</main>` then `<Footer />`
- Does NOT add 'use client' (layouts should be Server Components)
- Does NOT duplicate the html/body tags (parent layout already has those)
- Does NOT add NextIntlClientProvider (parent layout already provides it)

**Moving (auth):**

Move `app/[locale]/(auth)/` to `app/[locale]/(public)/(auth)/` so the login page inherits the public navigation. The URL stays `/login` since both route groups use parentheses.

Verify the middleware.ts login detection still works after the move -- it matches URL paths, not file paths, so it should be unaffected. The URL remains `/{locale}/login`.
  </action>
  <verify>
1. Run `pnpm build` -- must succeed with no errors
2. Verify `app/[locale]/(public)/layout.tsx` exists and imports Navigation + Footer
3. Verify `app/[locale]/(public)/(auth)/login/page.tsx` exists (login page moved under public)
4. Verify old `app/[locale]/(auth)/` directory no longer exists
5. Verify `components/landing/navigation.tsx` has 'use client', uses useState, has responsive breakpoints (md:hidden, hidden md:flex)
  </verify>
  <done>
Public route group layout exists with responsive navigation (sticky, hamburger on mobile, desktop links) and footer. Login page is nested under (public) route group and inherits the public navigation. Build passes cleanly.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with no errors
2. Medical color CSS variables exist in globals.css (--medical-blue-*, --medical-green-*, --medical-teal-*)
3. Both translation files have complete "landing" section
4. (public) route group layout renders navigation + main + footer
5. Navigation is responsive (hamburger on mobile)
6. Login page accessible at same URL path (/login, /en/login)
</verification>

<success_criteria>
- globals.css has 10+ medical color CSS variables with Tailwind theme mappings
- ar.json and en.json each have 25+ landing page translation keys
- (public)/layout.tsx renders PublicNavigation and Footer around children
- Navigation shows clinic name, 3 section links, login button, language toggle
- Navigation collapses to hamburger below md breakpoint
- Footer shows clinic name and copyright
- Login page still works at /login after relocation under (public)
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page-ui-shell/02-01-SUMMARY.md`
</output>
