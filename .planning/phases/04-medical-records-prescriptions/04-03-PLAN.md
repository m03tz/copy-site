---
phase: 04-medical-records-prescriptions
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - components/patients/patient-search.tsx
  - components/patients/patient-card.tsx
  - components/patients/patient-list.tsx
  - components/patients/patient-info-form.tsx
  - app/[locale]/doctor/patients/page.tsx
  - app/[locale]/doctor/patients/[id]/page.tsx
  - app/[locale]/secretary/patients/page.tsx
  - app/[locale]/secretary/patients/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Doctor and secretary can see a list of patients displayed as cards"
    - "Doctor and secretary can search patients by name or phone number"
    - "Patient profile page shows tabs: info, visits, prescriptions, files"
    - "Doctor and secretary can edit patient personal information from info tab"
    - "Patient list is sorted alphabetically by Arabic name by default"
  artifacts:
    - path: "components/patients/patient-search.tsx"
      provides: "Debounced search input that updates URL params"
      contains: "useDebouncedCallback"
    - path: "components/patients/patient-card.tsx"
      provides: "Patient card component for list display"
      contains: "Card"
    - path: "components/patients/patient-list.tsx"
      provides: "Grid of patient cards with pagination controls"
      contains: "PatientCard"
    - path: "components/patients/patient-info-form.tsx"
      provides: "Editable patient demographics form"
      contains: "updatePatientInfo"
    - path: "app/[locale]/doctor/patients/page.tsx"
      provides: "Doctor patient list page with search"
      contains: "searchPatients"
    - path: "app/[locale]/doctor/patients/[id]/page.tsx"
      provides: "Doctor patient profile page with tabs"
      contains: "TabsTrigger"
    - path: "app/[locale]/secretary/patients/page.tsx"
      provides: "Secretary patient list page (same UI as doctor)"
      contains: "searchPatients"
    - path: "app/[locale]/secretary/patients/[id]/page.tsx"
      provides: "Secretary patient profile page with tabs"
      contains: "TabsTrigger"
  key_links:
    - from: "components/patients/patient-search.tsx"
      to: "URL searchParams"
      via: "useDebouncedCallback updates URL with router.replace"
      pattern: "replace.*pathname"
    - from: "app/[locale]/doctor/patients/page.tsx"
      to: "lib/actions/patients.ts"
      via: "searchPatients server action call"
      pattern: "searchPatients"
    - from: "components/patients/patient-info-form.tsx"
      to: "lib/actions/patients.ts"
      via: "updatePatientInfo server action"
      pattern: "updatePatientInfo"
---

<objective>
Build the patient list with search and the patient profile page with tabbed layout for both doctor and secretary views.

Purpose: This creates the primary navigation surface for Phase 4 -- doctor and secretary need to find patients and access their records. The tabbed profile page is the container that later plans (visits, prescriptions, files) will populate with content.
Output: Patient search/list components, patient profile page with tabs, patient info edit form. Both doctor and secretary get the same UI.
</objective>

<execution_context>
@C:\Users\computer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\computer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-medical-records-prescriptions/04-RESEARCH.md
@.planning/phases/04-medical-records-prescriptions/04-02-SUMMARY.md
@lib/actions/patients.ts
@lib/types/database.ts
@messages/ar.json
@messages/en.json
@app/[locale]/doctor/layout.tsx
@app/[locale]/secretary/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create patient list components (search, card, list with pagination)</name>
  <files>
    components/patients/patient-search.tsx
    components/patients/patient-card.tsx
    components/patients/patient-list.tsx
  </files>
  <action>
    **components/patients/patient-search.tsx** (Client Component):
    - 'use client' directive
    - Uses `useDebouncedCallback` from 'use-debounce' (300ms debounce)
    - Reads current query from useSearchParams, updates URL via useRouter().replace
    - On input change: set page=1, set query param (or delete if empty)
    - Renders Input with Search icon (lucide-react) positioned at start (RTL-compatible with ps-10)
    - Accept `placeholder` prop (i18n translation passed from parent)

    **components/patients/patient-card.tsx** (Server or Client Component):
    - Displays patient as a Card (per user decision "Patient list displayed as cards"):
      - Patient name (full_name_ar primary, full_name_en secondary if exists)
      - Phone number (wrapped in dir="ltr" for correct digit order in RTL)
      - Date of birth
      - Blood type badge (if available)
    - Entire card is a Link to the patient profile page `/doctor/patients/{id}` or `/secretary/patients/{id}` (accept basePath prop)
    - Use logical CSS properties (ms/me, text-start) for RTL compatibility

    **components/patients/patient-list.tsx** (Client or Server Component):
    - Renders a responsive grid of PatientCard components (3 cols on lg, 2 on md, 1 on sm)
    - Pagination controls at bottom: Previous/Next buttons with page indicator
    - Pagination updates URL page param
    - Show total count
    - Empty state message when no patients found
    - Accept patients array, count, page, perPage, basePath as props
  </action>
  <verify>
    - All three component files exist under components/patients/
    - patient-search.tsx imports from 'use-debounce'
    - patient-card.tsx renders a Card component
    - `pnpm build` passes
  </verify>
  <done>
    Patient search component debounces input and updates URL params. Patient cards display name, phone, DOB, blood type. Patient list renders a responsive grid with pagination controls. All RTL-compatible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create patient list pages and profile pages with tabs</name>
  <files>
    app/[locale]/doctor/patients/page.tsx
    app/[locale]/doctor/patients/[id]/page.tsx
    app/[locale]/secretary/patients/page.tsx
    app/[locale]/secretary/patients/[id]/page.tsx
    components/patients/patient-info-form.tsx
  </files>
  <action>
    **app/[locale]/doctor/patients/page.tsx** (Server Component):
    - Read searchParams (query, page) from page props (Next.js 15: searchParams is a Promise)
    - Call searchPatients server action (or direct Supabase query) with query and page
    - Render PatientSearch + PatientList with basePath="/doctor/patients"
    - Use getTranslations('patients') for page title and labels

    **app/[locale]/secretary/patients/page.tsx** (Server Component):
    - Same as doctor version but with basePath="/secretary/patients"
    - Secretary sees the same patient list and search UI

    **app/[locale]/doctor/patients/[id]/page.tsx** (Server Component):
    - Read params.id (Next.js 15: params is a Promise)
    - Fetch patient profile: supabase.from('profiles').select('*, patients(*)').eq('id', id).single()
    - Fetch visit records: supabase.from('medical_records').select('*, prescriptions(*)').eq('patient_id', id).order('visit_date', { ascending: false })
    - Fetch files: supabase.from('patient_files').select('*').eq('patient_id', id).order('created_at', { ascending: false })
    - Render Tabs with 4 triggers: info | visits | prescriptions | files (per user decision)
    - Info tab: render PatientInfoForm with patient data
    - Visits tab: placeholder text "Visit records will appear here" (populated by Plan 04)
    - Prescriptions tab: placeholder text "Prescriptions will appear here" (populated by Plan 04)
    - Files tab: placeholder text "Files will appear here" (populated by Plan 05)
    - Pass fetched data as props so later plans can wire it into tab content

    **app/[locale]/secretary/patients/[id]/page.tsx** (Server Component):
    - Same tabbed layout as doctor version
    - Secretary can see all 4 tabs (per user decisions, secretary can create visit records and manage files)

    **components/patients/patient-info-form.tsx** (Client Component):
    - 'use client' directive
    - Editable form for patient demographics (per user decision "Both doctor and secretary can edit patient personal information"):
      - full_name_ar (required), full_name_en (optional), phone (required), email (optional)
      - date_of_birth, blood_type (select from common blood types), national_id
      - emergency_contact_name, emergency_contact_phone, notes (textarea)
    - Toggle between view mode and edit mode (Edit button switches to form inputs)
    - On save: call updatePatientInfo server action with FormData
    - Show success/error feedback
    - Use translations from 'patients' namespace
  </action>
  <verify>
    - All 4 page files and the info form component exist
    - Doctor and secretary patients pages render PatientSearch and PatientList
    - Profile pages use Tabs component with 4 tab triggers
    - PatientInfoForm calls updatePatientInfo action
    - `pnpm build` passes
  </verify>
  <done>
    Doctor and secretary both have patient list pages with search and pagination. Patient profile pages show tabbed layout (info/visits/prescriptions/files). Info tab has editable patient demographics form. Visits, prescriptions, and files tabs have placeholder content ready for Plans 04 and 05.
  </done>
</task>

</tasks>

<verification>
- Navigate to /doctor/patients -- see patient list with search bar and card layout
- Search by name or phone -- results filter via URL params
- Click a patient card -- navigate to profile page with 4 tabs
- Info tab shows patient details with edit capability
- Same functionality exists at /secretary/patients
- `pnpm build` passes
</verification>

<success_criteria>
- Patient list displays as cards (not table) per user decision
- Search works with debounce and URL params
- Default sort is alphabetical by Arabic name per user decision
- Profile page has 4 tabs: info, visits, prescriptions, files
- Patient info is editable by doctor and secretary
- Pagination works with page param in URL
</success_criteria>

<output>
After completion, create `.planning/phases/04-medical-records-prescriptions/04-03-SUMMARY.md`
</output>
