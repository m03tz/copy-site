---
phase: 06-dashboard-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00003_phase6_email_columns.sql
  - lib/types/database.ts
  - messages/ar.json
  - messages/en.json
autonomous: true

must_haves:
  truths:
    - "Appointments table has columns to store Resend email IDs for reminder cancellation"
    - "Database types reflect the new reminder email ID columns"
    - "shadcn Command component is installed and available"
    - "resend package is installed"
    - "Dashboard i18n keys exist in both Arabic and English for stat cards, day list, search, and navigation"
  artifacts:
    - path: "supabase/migrations/00003_phase6_email_columns.sql"
      provides: "SQL migration adding reminder_24h_email_id and reminder_2h_email_id to appointments"
      contains: "ALTER TABLE appointments"
    - path: "lib/types/database.ts"
      provides: "Updated TypeScript types with email ID columns on appointments"
      contains: "reminder_24h_email_id"
    - path: "messages/ar.json"
      provides: "Arabic translations for dashboard stat cards, day list, search, nav arrows"
      contains: "totalPatients"
    - path: "messages/en.json"
      provides: "English translations matching Arabic keys"
      contains: "totalPatients"
    - path: "components/ui/command.tsx"
      provides: "shadcn Command component for autocomplete"
  key_links:
    - from: "lib/types/database.ts"
      to: "supabase/migrations/00003_phase6_email_columns.sql"
      via: "TypeScript types mirror the SQL schema changes"
      pattern: "reminder_24h_email_id.*string.*null"
---

<objective>
Install dependencies (resend, shadcn command), create database migration for email reminder columns, update TypeScript types, and add all Phase 6 i18n translations.

Purpose: Provides the foundation layer (packages, types, translations, schema) that the dashboard UI plan and email integration plan both depend on.
Output: Migration SQL file, updated database.ts types, updated ar.json/en.json, installed resend + command packages
</objective>

<execution_context>
@C:\Users\computer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\computer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-notifications/06-RESEARCH.md
@lib/types/database.ts
@messages/ar.json
@messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install resend package and shadcn command component</name>
  <files>package.json, components/ui/command.tsx</files>
  <action>
Install the two new dependencies needed for Phase 6:

1. **Install resend** (email sending + scheduling):
   ```bash
   /c/Users/computer/AppData/Roaming/npm/pnpm add resend
   ```

2. **Install shadcn Command component** (autocomplete dropdown):
   ```bash
   /c/Users/computer/AppData/Roaming/npm/pnpm dlx shadcn@latest add command
   ```
   This will create `components/ui/command.tsx` and may install `cmdk` as a dependency.

3. **Verify both installed** by checking package.json contains `resend` and `cmdk` entries.

Note: pnpm is NOT on PATH in the shell. Must use the full path `/c/Users/computer/AppData/Roaming/npm/pnpm` for all pnpm commands (per decision NEW 04-01).
  </action>
  <verify>
- `package.json` contains `"resend"` dependency
- `package.json` contains `"cmdk"` dependency
- `components/ui/command.tsx` exists
- `/c/Users/computer/AppData/Roaming/npm/pnpm ls resend` shows installed version
  </verify>
  <done>resend and shadcn command component are installed and available for import in subsequent plans.</done>
</task>

<task type="auto">
  <name>Task 2: Database migration, TypeScript types, and i18n translations</name>
  <files>
    supabase/migrations/00003_phase6_email_columns.sql,
    lib/types/database.ts,
    messages/ar.json,
    messages/en.json
  </files>
  <action>
**1. Create `supabase/migrations/00003_phase6_email_columns.sql`**

Add two nullable text columns to the appointments table for storing Resend email IDs. This enables cancelling scheduled reminder emails when an appointment is cancelled.

```sql
-- Phase 6: Email reminder tracking columns
-- Stores Resend email IDs so scheduled reminders can be cancelled
-- when an appointment is cancelled

ALTER TABLE appointments
  ADD COLUMN reminder_24h_email_id TEXT,
  ADD COLUMN reminder_2h_email_id TEXT;

-- Note: Apply via Supabase Dashboard SQL Editor
```

Per research recommendation, use Option A (columns on appointments table) for simplicity at this clinic's scale.

**2. Update `lib/types/database.ts`**

Add the two new columns to the appointments type definitions:

- In `appointments.Row`: add `reminder_24h_email_id: string | null` and `reminder_2h_email_id: string | null`
- In `appointments.Insert`: add `reminder_24h_email_id?: string | null` and `reminder_2h_email_id?: string | null`
- In `appointments.Update`: add `reminder_24h_email_id?: string | null` and `reminder_2h_email_id?: string | null`

Place them after the `updated_at` field in each sub-type, maintaining the existing code style.

**3. Update `messages/ar.json`** — Add new dashboard keys

Expand the `"dashboard"` object with the following new keys (keep all existing keys):

```json
"dashboard": {
  "doctorTitle": "لوحة تحكم الطبيب",
  "secretaryTitle": "لوحة تحكم السكرتيرة",
  "patientTitle": "لوحة تحكم المريض",
  "placeholder": "قريباً...",
  "dueDateAlerts": "مواعيد ولادة قريبة",
  "daysRemaining": "يوم متبقٍ",
  "dueToday": "موعد الولادة اليوم",
  "noDueDateAlerts": "لا توجد مواعيد ولادة قريبة",
  "todayAppointments": "مواعيد اليوم",
  "totalPatients": "إجمالي المرضى",
  "approachingDueDate": "حالات ولادة قريبة",
  "noAppointmentsToday": "لا توجد مواعيد اليوم",
  "searchPatients": "ابحث عن مريض...",
  "searchNoResults": "لا توجد نتائج",
  "searchLoading": "جارٍ البحث...",
  "previousDay": "اليوم السابق",
  "nextDay": "اليوم التالي",
  "appointmentsFor": "مواعيد يوم"
}
```

**4. Update `messages/en.json`** — Add matching English keys

Expand the `"dashboard"` object with matching English translations:

```json
"dashboard": {
  "doctorTitle": "Doctor Dashboard",
  "secretaryTitle": "Secretary Dashboard",
  "patientTitle": "Patient Dashboard",
  "placeholder": "Coming soon...",
  "dueDateAlerts": "Upcoming Due Dates",
  "daysRemaining": "days remaining",
  "dueToday": "Due today",
  "noDueDateAlerts": "No upcoming due dates",
  "todayAppointments": "Today's Appointments",
  "totalPatients": "Total Patients",
  "approachingDueDate": "Approaching Due Date",
  "noAppointmentsToday": "No appointments today",
  "searchPatients": "Search for a patient...",
  "searchNoResults": "No results found",
  "searchLoading": "Searching...",
  "previousDay": "Previous day",
  "nextDay": "Next day",
  "appointmentsFor": "Appointments for"
}
```

Preserve all other existing keys in both JSON files. Only modify the `"dashboard"` section.
  </action>
  <verify>
- `supabase/migrations/00003_phase6_email_columns.sql` exists with ALTER TABLE statement
- `lib/types/database.ts` appointments Row/Insert/Update all include `reminder_24h_email_id` and `reminder_2h_email_id`
- `messages/ar.json` dashboard section has keys: todayAppointments, totalPatients, approachingDueDate, noAppointmentsToday, searchPatients, searchNoResults, searchLoading, previousDay, nextDay, appointmentsFor
- `messages/en.json` dashboard section has all the same keys with English values
- `npx tsc --noEmit` passes (types are valid)
  </verify>
  <done>Database migration file ready for SQL Editor application, TypeScript types updated with email ID columns, and all Phase 6 dashboard i18n translations added in both Arabic and English.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- resend and cmdk packages appear in package.json dependencies
- components/ui/command.tsx exists
- supabase/migrations/00003_phase6_email_columns.sql exists
- lib/types/database.ts has reminder_24h_email_id and reminder_2h_email_id in appointments types
- messages/ar.json and messages/en.json dashboard sections have all new keys
</verification>

<success_criteria>
- All Phase 6 dependencies installed (resend, shadcn command)
- Database migration SQL ready for application
- TypeScript types mirror new schema columns
- i18n translations cover all dashboard UI elements planned in Phase 6
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-notifications/06-01-SUMMARY.md`
</output>
