---
phase: 01-foundation-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - tsconfig.json
  - .env.local
  - .env.example
  - .gitignore
  - app/layout.tsx
  - app/page.tsx
  - components.json
  - lib/utils.ts
  - app/globals.css
autonomous: true
user_setup:
  - service: supabase
    why: "Database and authentication backend"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role secret key"
    dashboard_config:
      - task: "Create a new Supabase project"
        location: "https://supabase.com/dashboard -> New project"

must_haves:
  truths:
    - "Next.js 15 dev server starts without errors"
    - "All required dependencies are installed (supabase, next-intl, zod, react-hook-form)"
    - "Tailwind CSS 4 and shadcn/ui are configured and functional"
    - "Environment variables template exists for Supabase connection"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "@supabase/ssr"
    - path: "next.config.ts"
      provides: "Next.js configuration with next-intl plugin"
      contains: "next-intl"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: "app/globals.css"
      provides: "Global styles with Tailwind"
      contains: "@tailwind"
  key_links:
    - from: "next.config.ts"
      to: "next-intl"
      via: "createNextIntlPlugin import"
      pattern: "createNextIntlPlugin"
---

<objective>
Scaffold the Next.js 15 project with all dependencies, Tailwind CSS 4, shadcn/ui, and environment configuration for the clinic management system.

Purpose: Establish the working development environment that all subsequent plans build upon. Without a running Next.js app with correct deps, nothing else can proceed.
Output: A working Next.js 15 app that starts with `pnpm dev`, with all required libraries installed and configured.
</objective>

<execution_context>
@C:\Users\computer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\computer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 app and install all dependencies</name>
  <files>
    package.json
    pnpm-lock.yaml
    next.config.ts
    tsconfig.json
    .gitignore
    app/layout.tsx
    app/page.tsx
    app/globals.css
  </files>
  <action>
    1. Create Next.js 15 project using `pnpm create next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"` in the project root. If the directory is not empty, initialize manually or use `--yes` flag as appropriate.

    2. Install all required dependencies:
       ```
       pnpm add @supabase/supabase-js @supabase/ssr next-intl zod react-hook-form @hookform/resolvers date-fns-tz
       pnpm add -D supabase
       ```

    3. Update `next.config.ts` to integrate the next-intl plugin:
       ```typescript
       import createNextIntlPlugin from 'next-intl/plugin'

       const withNextIntl = createNextIntlPlugin()

       const nextConfig = {}

       export default withNextIntl(nextConfig)
       ```

    4. Verify `tsconfig.json` has the `@/*` path alias configured (should be set by create-next-app).

    5. Update `.gitignore` to include `.env.local` and `.env*.local` (should already be there from create-next-app, but verify).

    IMPORTANT: Do NOT use `src/` directory. The project uses root-level `app/` directory per project conventions.
    IMPORTANT: Use pnpm (not npm or yarn) as the package manager per project decision.
  </action>
  <verify>
    Run `pnpm dev` and confirm the dev server starts on http://localhost:3000 without errors.
    Run `pnpm build` and confirm it compiles successfully.
    Verify `node_modules/@supabase/ssr` exists.
    Verify `node_modules/next-intl` exists.
  </verify>
  <done>
    Dev server starts cleanly. All 7 required packages appear in package.json dependencies. Build succeeds without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure shadcn/ui and environment variables template</name>
  <files>
    components.json
    lib/utils.ts
    .env.local
    .env.example
    app/globals.css
  </files>
  <action>
    1. Initialize shadcn/ui using `pnpm dlx shadcn@latest init`. When prompted:
       - Style: Default
       - Base color: Slate (medical-appropriate neutral)
       - CSS variables: Yes
       - Use `@/` import alias

    2. This creates `components.json` and `lib/utils.ts` with the `cn()` utility function.

    3. Verify `app/globals.css` has the shadcn/ui CSS variables (the init command should update it).

    4. Create `.env.local` with placeholder values:
       ```
       NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
       NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
       SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
       ```

    5. Create `.env.example` with the same keys but no values (for documentation):
       ```
       # Supabase Configuration
       # Get these from: Supabase Dashboard -> Project Settings -> API
       NEXT_PUBLIC_SUPABASE_URL=
       NEXT_PUBLIC_SUPABASE_ANON_KEY=
       SUPABASE_SERVICE_ROLE_KEY=
       ```

    6. Install a few essential shadcn/ui components that will be used across the app:
       ```
       pnpm dlx shadcn@latest add button input label card
       ```

    IMPORTANT: shadcn/ui init may ask about Tailwind CSS 4 compatibility. Accept the defaults for Tailwind 4.
  </action>
  <verify>
    Verify `components.json` exists with correct config.
    Verify `lib/utils.ts` exports `cn` function.
    Verify `.env.example` has all 3 Supabase env vars listed.
    Verify `components/ui/button.tsx` exists (shadcn component installed).
    Run `pnpm build` to confirm everything compiles.
  </verify>
  <done>
    shadcn/ui initialized with Button, Input, Label, Card components available. Environment template exists. Build passes with all configuration in place.
  </done>
</task>

</tasks>

<verification>
1. `pnpm dev` starts without errors
2. `pnpm build` completes successfully
3. All required packages in `package.json`: @supabase/ssr, @supabase/supabase-js, next-intl, zod, react-hook-form, @hookform/resolvers, date-fns-tz
4. `components.json` exists with shadcn/ui configuration
5. `.env.example` documents all required environment variables
6. No `src/` directory exists (root-level `app/` used)
</verification>

<success_criteria>
- Next.js 15 dev server starts and renders default page
- All 7 runtime + 1 dev dependencies installed
- shadcn/ui configured with Button, Input, Label, Card components
- Tailwind CSS 4 functional
- Environment variable template created
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security/01-01-SUMMARY.md`
</output>
