---
phase: 02-landing-page-ui-shell
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/landing/hero.tsx
  - components/landing/services.tsx
  - components/landing/contact.tsx
  - app/[locale]/(public)/page.tsx
  - app/[locale]/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visitor sees Dr. Fadi's name, specialty, credentials, and bio on landing page"
    - "Visitor sees 4 clinic services with icons and descriptions"
    - "Visitor sees clinic address, phone number, and working hours"
    - "Visitor can click CTA to navigate to login page"
    - "Landing page displays correctly in Arabic (RTL) and English (LTR)"
    - "Page has proper SEO metadata (title, description, OpenGraph) in both languages"
  artifacts:
    - path: "components/landing/hero.tsx"
      provides: "Hero section with doctor bio and CTA"
      min_lines: 30
    - path: "components/landing/services.tsx"
      provides: "4-service grid with icons"
      min_lines: 40
    - path: "components/landing/contact.tsx"
      provides: "Contact section with address, phone, hours"
      min_lines: 40
    - path: "app/[locale]/(public)/page.tsx"
      provides: "Landing page composing hero, services, contact with SEO metadata"
      min_lines: 30
  key_links:
    - from: "app/[locale]/(public)/page.tsx"
      to: "components/landing/hero.tsx"
      via: "import Hero"
      pattern: "import.*Hero.*hero"
    - from: "app/[locale]/(public)/page.tsx"
      to: "components/landing/services.tsx"
      via: "import Services"
      pattern: "import.*Services.*services"
    - from: "app/[locale]/(public)/page.tsx"
      to: "components/landing/contact.tsx"
      via: "import Contact"
      pattern: "import.*Contact.*contact"
    - from: "components/landing/hero.tsx"
      to: "/login"
      via: "CTA Link to login"
      pattern: "href.*login"
    - from: "app/[locale]/(public)/page.tsx"
      to: "generateMetadata"
      via: "exported async function"
      pattern: "export.*generateMetadata"
---

<objective>
Build the landing page content components (hero, services, contact) and wire them into the landing page with locale-based SEO metadata.

Purpose: This plan delivers the actual visible landing page content that visitors see -- the doctor's bio and credentials, the 4 services offered, clinic contact information, and proper SEO tags. This is the core deliverable of Phase 2.

Output: Three landing page section components, a complete landing page at /, and SEO metadata for both Arabic and English.
</objective>

<execution_context>
@C:\Users\computer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\computer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page-ui-shell/02-RESEARCH.md
@.planning/phases/02-landing-page-ui-shell/02-01-SUMMARY.md

# Existing files for reference
@app/[locale]/layout.tsx
@components/language-toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero, Services, and Contact landing page components</name>
  <files>
    components/landing/hero.tsx
    components/landing/services.tsx
    components/landing/contact.tsx
  </files>
  <action>
All three components use `useTranslations('landing')` from 'next-intl' (client-side) since they need translation hooks. Mark each with `'use client'`.

**components/landing/hero.tsx -- Hero section:**

Create a `Hero` component with:
- Full-width section with `bg-gradient-to-b from-medical-blue-50 to-white py-16 sm:py-24`
- Max-w-7xl centered container with px-4
- Two-column grid on md+ (`grid md:grid-cols-2 gap-12 items-center`), stacks on mobile
- Left column (content):
  - Small badge/label: `t('aboutDoctor')` in `text-medical-blue-600 font-semibold text-sm uppercase tracking-wide`
  - Doctor name: `t('clinicName')` in `text-4xl sm:text-5xl font-bold text-gray-900` (this is "Dr. Fadi Nadi Al-Sahleh Clinic" -- doctor name is the clinic name)
  - Specialty: `t('heroTitle')` in `text-xl text-gray-600 mt-4`
  - Subtitle: `t('heroSubtitle')` in `text-lg text-gray-500 mt-2`
  - CTA button: `<Link href="/login">` from `@/i18n/routing`, styled as `inline-block mt-8 px-8 py-4 bg-medical-blue-600 text-white text-lg rounded-lg hover:bg-medical-blue-700 transition-colors shadow-lg`, text: `t('bookAppointment')`
- Right column (credentials):
  - A card-like div with `bg-white rounded-2xl shadow-lg p-8 border border-medical-blue-100`
  - Section title: `t('aboutDoctor')` in `text-2xl font-bold text-gray-900 mb-4`
  - Bio text: `t('aboutDoctorText')` in `text-gray-600 leading-relaxed`
  - A subtle decorative element: `div` with `w-16 h-1 bg-medical-blue-500 rounded mt-6`
- Use LOGICAL properties: `ms-*` not `ml-*`, `me-*` not `mr-*`, `text-start` not `text-left`
- Add `id="about"` to the section element so the nav anchor link #about scrolls here

**components/landing/services.tsx -- Services grid:**

Create a `Services` component with:
- Section with `py-16 bg-white` and `id="services"` for anchor linking
- Max-w-7xl centered container
- Title: `t('servicesTitle')` in `text-3xl font-bold text-center mb-12 text-gray-900`
- 4-item grid: `grid sm:grid-cols-2 lg:grid-cols-4 gap-8`
- Each service card:
  - `bg-medical-blue-50 p-6 rounded-xl hover:shadow-md transition-shadow border border-transparent hover:border-medical-blue-100`
  - Icon (from lucide-react) in `w-12 h-12 text-medical-blue-600 mb-4`
  - Title in `text-xl font-semibold mb-2 text-gray-900`
  - Description in `text-gray-600`
- Service data array (map with index key):
  1. Icon: `Hospital` from lucide-react, title: `t('serviceObgyn')`, desc: `t('serviceObgynDesc')`
  2. Icon: `Baby` from lucide-react, title: `t('servicePregnancy')`, desc: `t('servicePregnancyDesc')`
  3. Icon: `HeartPulse` from lucide-react, title: `t('serviceInfertility')`, desc: `t('serviceInfertilityDesc')`
  4. Icon: `Stethoscope` from lucide-react, title: `t('serviceSurgery')`, desc: `t('serviceSurgeryDesc')`

Note: Use `Hospital`, `Baby`, `HeartPulse`, `Stethoscope` icons from lucide-react (all available in the installed version). Do NOT use `BriefcaseMedical` or `Pill` -- `HeartPulse` and `Stethoscope` are more recognizable for infertility and surgery respectively.

**components/landing/contact.tsx -- Contact section:**

Create a `Contact` component with:
- Section with `py-16 bg-gray-50` and `id="contact"` for anchor linking
- Max-w-7xl centered container
- Title: `t('contactTitle')` in `text-3xl font-bold text-center mb-12 text-gray-900`
- Two-column grid on md+ (`grid md:grid-cols-2 gap-12`)
- Left column (contact info):
  - `space-y-6` container
  - Address row: `MapPin` icon (lucide-react) in `w-6 h-6 text-medical-blue-600 shrink-0 mt-1`, `t('address')` as label in `font-semibold`, `t('clinicAddress')` as value in `text-gray-600`
  - Phone row: `Phone` icon, `t('phone')` label, `t('clinicPhone')` as value -- wrap phone in `<a href="tel:+962XXXXXXXXX" dir="ltr" className="text-medical-blue-600 hover:underline">` (phone numbers always LTR)
  - Hours row: `Clock` icon, `t('workingHours')` label, `t('weekdayHours')` + `t('weekendHours')` values in `text-gray-600`
  - Each row: `flex gap-4` with icon and text div
- Right column (booking CTA card):
  - `bg-white rounded-2xl p-8 shadow-sm border border-medical-blue-100 flex flex-col items-center justify-center text-center`
  - Title: `t('bookingCTA')` in `text-2xl font-bold mb-4 text-gray-900`
  - Description: `t('bookingDescription')` in `text-gray-600 mb-6`
  - CTA Link: `<Link href="/login">` styled as `inline-block px-6 py-3 bg-medical-blue-600 text-white rounded-lg hover:bg-medical-blue-700 transition-colors`, text: `t('bookNow')`
  </action>
  <verify>
Run `pnpm build` to verify all three components compile without errors. Check that lucide-react icons import successfully. Verify each component has the correct id attribute for anchor linking (#about, #services, #contact).
  </verify>
  <done>
Three landing page section components exist: Hero (doctor bio + CTA), Services (4-item grid with icons), Contact (address, phone, hours + booking CTA). All use translations, logical properties, and medical color palette.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create landing page with SEO metadata and remove old placeholder</name>
  <files>
    app/[locale]/(public)/page.tsx
    app/[locale]/page.tsx
  </files>
  <action>
**Delete `app/[locale]/page.tsx`** (the old placeholder home page from Phase 1). This file is being replaced by the new landing page at `app/[locale]/(public)/page.tsx`. The URL stays the same (/) because (public) is a route group.

**Create `app/[locale]/(public)/page.tsx`:**

This is a Server Component (no 'use client') that:

1. Exports `generateMetadata` for locale-based SEO:
```typescript
import type { Metadata } from 'next'

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>
}): Promise<Metadata> {
  const { locale } = await params

  return {
    title: locale === 'ar'
      ? 'د. فادي نادي السحلة - أخصائي نساء وتوليد | جرش، الأردن'
      : 'Dr. Fadi Nadi Al-Sahleh - OB/GYN Specialist | Jerash, Jordan',
    description: locale === 'ar'
      ? 'عيادة متخصصة في أمراض النساء والتوليد والعقم والجراحة بالمنظار في جرش، الأردن. احجزي موعدك الآن.'
      : 'Specialized clinic for OB/GYN, infertility, and laparoscopic surgery in Jerash, Jordan. Book your appointment now.',
    openGraph: {
      title: locale === 'ar'
        ? 'عيادة د. فادي نادي السحلة'
        : 'Dr. Fadi Nadi Al-Sahleh Clinic',
      description: locale === 'ar'
        ? 'أخصائي نساء وتوليد وعقم وجراحة بالمنظار'
        : 'OB/GYN, Infertility & Laparoscopic Surgery Specialist',
      locale: locale === 'ar' ? 'ar_JO' : 'en_US',
      type: 'website',
    },
  }
}
```

2. Default export renders the landing page sections in order:
```typescript
import { Hero } from '@/components/landing/hero'
import { Services } from '@/components/landing/services'
import { Contact } from '@/components/landing/contact'

export default function LandingPage() {
  return (
    <>
      <Hero />
      <Services />
      <Contact />
    </>
  )
}
```

The page is intentionally simple -- it composes the three section components. The navigation and footer come from the (public)/layout.tsx (created in Plan 01).

No props needed since components use useTranslations internally.
  </action>
  <verify>
1. Run `pnpm build` -- must succeed with zero errors
2. Verify `app/[locale]/page.tsx` does NOT exist (deleted)
3. Verify `app/[locale]/(public)/page.tsx` exists and imports Hero, Services, Contact
4. Verify generateMetadata is exported with Arabic and English titles
5. Start dev server (`pnpm dev`) and verify http://localhost:3000 renders the landing page with navigation, hero, services, contact, and footer
  </verify>
  <done>
Landing page at / renders hero (doctor bio + CTA), services (4-item grid), and contact (address/phone/hours) sections with SEO metadata. Old placeholder page deleted. Page works in both Arabic (/) and English (/en/).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify landing page visual quality</name>
  <what-built>Complete landing page with medical theme, responsive navigation, hero section (doctor bio and credentials), 4-service grid with icons, contact section with address/phone/hours, footer, and SEO metadata. Page works in Arabic (RTL) and English (LTR) with language toggle.</what-built>
  <how-to-verify>
1. Open http://localhost:3000 in browser -- should see Arabic (RTL) landing page
2. Verify navigation: clinic name, section links (About, Services, Contact), login button (blue), language toggle
3. Verify hero: Doctor name, specialty, subtitle, "Book Appointment" CTA button, credentials card
4. Verify services: 4 cards with icons (Hospital, Baby, HeartPulse, Stethoscope), Arabic titles and descriptions
5. Verify contact: Address (Jerash), phone number, working hours, booking CTA card
6. Verify footer: Clinic name and copyright
7. Click language toggle to switch to English -- verify all text changes, layout switches to LTR
8. Click "Book Appointment" or "Sign In" CTA -- should navigate to /login page
9. Resize browser to mobile width (< 768px) -- navigation should collapse to hamburger menu
10. Click hamburger -- mobile menu should show all links
11. Check browser tab -- should show proper Arabic/English title (SEO metadata)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with no errors
2. Landing page at / shows hero, services, contact sections
3. Landing page at /en/ shows English version
4. Navigation has clinic name, section links, login button, language toggle
5. Navigation is responsive (hamburger on mobile)
6. All 4 services display with icons
7. Contact section shows address, phone, hours
8. CTA buttons link to /login
9. Browser tab shows locale-appropriate title
10. Footer shows clinic name and copyright year
</verification>

<success_criteria>
- Hero section displays doctor name, specialty, credentials, and CTA
- Services section displays 4 items with lucide-react icons
- Contact section displays address, phone, working hours, and booking CTA
- generateMetadata exports proper Arabic and English metadata with OpenGraph
- Old placeholder page.tsx deleted, replaced by (public)/page.tsx
- Landing page renders correctly in both Arabic (RTL) and English (LTR)
- All CTA buttons navigate to /login
- Visual checkpoint passes (user approves appearance)
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page-ui-shell/02-02-SUMMARY.md`
</output>
