---
phase: 04-medical-records-prescriptions
plan: 05
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - components/files/file-upload.tsx
  - components/files/file-list.tsx
  - app/[locale]/doctor/patients/[id]/page.tsx
  - app/[locale]/secretary/patients/[id]/page.tsx
  - app/[locale]/patient/records/page.tsx
autonomous: true

must_haves:
  truths:
    - "Doctor or secretary can upload images and PDFs linked to a specific visit"
    - "Doctor or secretary can delete uploaded files"
    - "Files display as a list with name, upload date, and type"
    - "Patient can view their own visit history, prescriptions, and files read-only"
  artifacts:
    - path: "components/files/file-upload.tsx"
      provides: "File upload component with visit selection and type validation"
      contains: "uploadFile"
    - path: "components/files/file-list.tsx"
      provides: "File list display with download and delete actions"
      contains: "deleteFile"
    - path: "app/[locale]/patient/records/page.tsx"
      provides: "Patient read-only view of visits, prescriptions, and files"
      contains: "medical_records"
  key_links:
    - from: "components/files/file-upload.tsx"
      to: "lib/actions/files.ts"
      via: "uploadFile server action with FormData"
      pattern: "uploadFile"
    - from: "components/files/file-list.tsx"
      to: "lib/actions/files.ts"
      via: "deleteFile and getFileUrl server actions"
      pattern: "deleteFile|getFileUrl"
    - from: "app/[locale]/patient/records/page.tsx"
      to: "supabase.from('medical_records')"
      via: "Server Component Supabase query filtered to current patient"
      pattern: "from\\('medical_records'\\)"
---

<objective>
Build file upload/management UI for the patient profile files tab, and create the patient read-only records portal page showing their own visits, prescriptions, and files.

Purpose: Completes the clinical workflow (files attached to visits) and delivers the patient-facing portal where patients view their own medical history read-only. This satisfies requirements FILE-01, FILE-02, FILE-03, MEDR-02, PRSC-03.
Output: File upload/list components wired into profile files tab, patient records page with read-only view.
</objective>

<execution_context>
@C:\Users\computer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\computer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-medical-records-prescriptions/04-RESEARCH.md
@.planning/phases/04-medical-records-prescriptions/04-03-SUMMARY.md
@lib/actions/files.ts
@lib/types/database.ts
@app/[locale]/doctor/patients/[id]/page.tsx
@app/[locale]/secretary/patients/[id]/page.tsx
@app/[locale]/patient/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file upload and file list components, wire into profile files tab</name>
  <files>
    components/files/file-upload.tsx
    components/files/file-list.tsx
    app/[locale]/doctor/patients/[id]/page.tsx
    app/[locale]/secretary/patients/[id]/page.tsx
  </files>
  <action>
    **components/files/file-upload.tsx** (Client Component):
    - 'use client' directive
    - Form with: file input (accept="image/jpeg,image/png,image/webp,application/pdf"), hidden patient_id, medical_record_id select (dropdown of patient's visit records to link file to a specific visit per user decision "Files are linked to a specific visit")
    - Client-side validation: file type check, max size hint (10MB)
    - On submit: create FormData with file, patient_id, medical_record_id, call uploadFile server action
    - Show uploading state (spinner/progress indicator), success/error feedback
    - Display allowed file types and max size info
    - Use translations from 'files' namespace
    - Accept visits array as prop (to populate the visit selector dropdown)

    **components/files/file-list.tsx** (Client Component):
    - 'use client' directive
    - Display files as a list (per user decision "Files displayed as a file list, not gallery view"):
      - File name, upload date (formatted with date-fns), file type badge (image/pdf using Badge component)
    - No file categories/labels (per user decision "filename and date are sufficient")
    - Download action: calls getFileUrl to get signed URL, opens in new tab
    - Delete action: confirmation Dialog, then calls deleteFile server action (per user decision "Both doctor and secretary can delete uploaded files")
    - Empty state message when no files exist
    - Accept files array and patientId as props

    **Update patient profile pages** (both doctor and secretary):
    - Replace files tab placeholder with FileUpload + FileList components
    - Pass the already-fetched files data and visits data (for visit selector in upload form)
    - FileUpload component sits above FileList in the files tab
  </action>
  <verify>
    - Both file components exist under components/files/
    - file-upload.tsx has a visit selector (medical_record_id select)
    - file-list.tsx displays a list (not gallery) with download and delete actions
    - Profile pages render file components in the files tab
    - `pnpm build` passes
  </verify>
  <done>
    File upload component allows selecting a visit and uploading images/PDFs. File list displays files with name, date, type badge, download (signed URL), and delete (with confirmation). Files tab in patient profile is fully functional for doctor and secretary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create patient read-only records portal page</name>
  <files>
    app/[locale]/patient/records/page.tsx
  </files>
  <action>
    **app/[locale]/patient/records/page.tsx** (Server Component):
    - Get current user from Supabase auth
    - Fetch the patient's own medical records with prescriptions: supabase.from('medical_records').select('*, prescriptions(*)').eq('patient_id', user.id).order('visit_date', { ascending: false })
    - Fetch the patient's own files: supabase.from('patient_files').select('*').eq('patient_id', user.id).order('created_at', { ascending: false })
    - Render a chronological view of visits (per research recommendation: "single-page chronological view with visit cards with embedded prescriptions and files")
    - Each visit card shows: visit date, doctor notes, prescriptions (if any) as a medication list, files (if any) linked to that visit
    - Patient view is READ-ONLY: no edit buttons, no delete buttons, no upload form
    - Prescriptions show "Print" button (reuse PrescriptionPrint component -- patient can print their own prescriptions)
    - Files show "Download" button (calls getFileUrl for signed URL, opens in new tab)
    - Use translations from 'visits', 'prescriptions', 'files' namespaces
    - Empty state if patient has no records yet
    - generateMetadata for SEO (follow established pattern from existing pages)

    Note: The patient layout already has a nav item for /patient/records. This page fulfills that link.
  </action>
  <verify>
    - File exists at app/[locale]/patient/records/page.tsx
    - Page queries medical_records filtered by current user's patient_id
    - No edit/delete/upload actions exist on this page (read-only)
    - Prescription print button exists for patient to print their prescriptions
    - `pnpm build` passes
  </verify>
  <done>
    Patient can view their own visit history in chronological order with embedded prescriptions and files. View is completely read-only. Patient can print their prescriptions and download their files. No create/edit/delete actions available.
  </done>
</task>

</tasks>

<verification>
- File upload works: select visit, choose file, upload succeeds
- File list shows uploaded files with name, date, type
- File download opens signed URL in new tab
- File delete removes from storage and list (with confirmation)
- Patient records page shows own visits, prescriptions, files read-only
- Patient can print prescriptions from their portal
- `pnpm build` passes
</verification>

<success_criteria>
- Files linked to specific visits (not patient-level)
- File list display (not gallery) per user decision
- Both doctor and secretary can upload and delete files
- Patient portal shows read-only chronological view of visits + prescriptions + files
- Patient can print prescriptions and download files
- No edit/delete actions visible to patient role
</success_criteria>

<output>
After completion, create `.planning/phases/04-medical-records-prescriptions/04-05-SUMMARY.md`
</output>
